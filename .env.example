# Root docker-compose development placeholders.
# Copy to .env for local development only. Never commit .env.

POSTGRES_DB=agriconto
POSTGRES_USER=agriconto
POSTGRES_PASSWORD=agriconto_dev_password_change_me

# Development-only placeholder. Production must use a unique random value with at least 32 bytes.
JWT_SECRET_KEY=dev-only-change-this-secret-minimum-32-characters
