# Features

Feature modules will live here as the product grows. Each feature should keep UI state close to the feature and call shared API/auth/lib utilities instead of bypassing backend permissions.

